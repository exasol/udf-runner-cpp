# A template to use Docker (rootful) instead of containerd & nerdctl
# And provide all dependencies necessary to build the Exasol UDF client with Bazel
# Also, it sets all environment variables needed to build the UDF client for Python and Java
# TODO: Add R dependencies
# Start this VM when the host has no Docker daemon:
# $ limactl start ./ext/lima_vm_templates/docker-udf-client.yaml
#
# Connect Docker and exaslct to the Lima Docker daemon:
# $ export DOCKER_HOST=$(limactl list docker-udf-client --format 'unix://{{.Dir}}/sock/docker.sock')
# cd ./exaudfclient
# $ limactl shell docker-udf-client bazel build --config no-tty --config optimize --config python --config fast-binary

# To build the SLC Docker images/stages only, run
# $ exaslct build --flavor-path ./flavors/<flavor-name>
#
# To export a deployable SLC archive, run
# $ exaslct export --flavor-path ./flavors/<flavor-name> --export-path .build_output/slc
#
# To run a focused database test, use --test-file or --test-folder, for example:
# $ exaslct run-db-test --flavor-path ./flavors/<flavor-name> \
#     --test-container-folder ./test_container \
#     --test-folder ./test_container/tests/test/smoke

minimumLimaVersion: 1.1.0

base:
- template://_images/ubuntu-24.04
- template://_default/mounts

# containerd is managed by Docker, not by Lima, so the values are set to false here.
containerd:
  system: false
  user: false
provision:
- mode: system
  # This script defines the host.docker.internal hostname when hostResolver is disabled.
  # It is also needed for lima 0.8.2 and earlier, which does not support hostResolver.hosts.
  # Names defined in /etc/hosts inside the VM are not resolved inside containers when
  # using the hostResolver; use hostResolver.hosts instead (requires lima 0.8.3 or later).
  script: |
    #!/bin/sh
    sed -i 's/host.lima.internal.*/host.lima.internal host.docker.internal/' /etc/hosts
    sysctl -w kernel.apparmor_restrict_unprivileged_userns=0
- mode: system
  script: |
    #!/bin/bash
    set -eux -o pipefail
    command -v docker >/dev/null 2>&1 && exit 0
    if [ ! -e /etc/systemd/system/docker.socket.d/override.conf ]; then
      mkdir -p /etc/systemd/system/docker.socket.d
      # Alternatively we could just add the user to the "docker" group, but that requires restarting the user session
      cat <<-EOF >/etc/systemd/system/docker.socket.d/override.conf
      [Socket]
      SocketUser={{.User}}
    EOF
    fi
    export DEBIAN_FRONTEND=noninteractive
    curl -fsSL https://get.docker.com | sh
- mode: system
  script: |
   #!/bin/bash
   set -eux -o pipefail
   export DEBIAN_FRONTEND=noninteractive
   curl -1sLf 'https://dl.cloudsmith.io/public/mull-project/mull-stable/setup.deb.sh' | bash
   apt update && apt install -y protobuf-compiler libzmq3-dev openjdk-17-jdk build-essential git python3.12-dev python3-pip libpcre3-dev clang-tidy-20 lcov
   apt install -y clang-20 mull-20=0.34.1
   mull-runner-20 --version
   test -x /usr/lib/mull-ir-frontend-20
   curl -L  https://github.com/bazelbuild/bazelisk/releases/download/v1.27.0/bazelisk-linux-amd64 -o /usr/bin/bazel
   chmod +x /usr/bin/bazel
   pip install --break-system-packages numpy
   curl -L -o swig-2.0.4.tar.gz https://exasol-script-languages-dependencies.s3.eu-central-1.amazonaws.com/swig-2.0.4.tar.gz && tar zxf swig-2.0.4.tar.gz && (cd swig-2.0.4 && ./configure --prefix=/usr && make && make install) && rm -rf swig-2.0.4 swig-2.0.4.tar.gz
   poetry_home=/opt/poetry
   if [ ! -x "$poetry_home/bin/poetry" ]; then
     curl -sSL https://install.python-poetry.org | POETRY_HOME="$poetry_home" python3 - --version 2.3.0
   fi
   ln -sfn "$poetry_home/bin/poetry" /usr/local/bin/poetry
   /usr/local/bin/poetry --version
- mode: system
  script: |
   #!/bin/sh
   echo 'export PATH="/opt/poetry/bin:$PATH"' > /etc/profile.d/poetry.sh
   chmod +x /etc/profile.d/poetry.sh
probes:
- script: |
    #!/bin/bash
    set -eux -o pipefail
    if ! timeout 30s bash -c "until command -v docker >/dev/null 2>&1; do sleep 3; done"; then
      echo >&2 "docker is not installed yet"
      exit 1
    fi
    if ! timeout 30s bash -c "until pgrep dockerd; do sleep 3; done"; then
      echo >&2 "dockerd is not running"
      exit 1
    fi
  hint: See "/var/log/cloud-init-output.log" in the guest
hostResolver:
  # hostResolver.hosts requires lima 0.8.3 or later. Names defined here will also
  # resolve inside containers, and not just inside the VM itself.
  hosts:
    host.docker.internal: host.lima.internal
portForwards:
- guestSocket: "/var/run/docker.sock"
  hostSocket: "{{.Dir}}/sock/docker.sock"
message: |
  To run `docker` on the host (assumes docker-cli is installed), run the following commands:
  ------
  docker context create lima-{{.Name}} --docker "host=unix://{{.Dir}}/sock/docker.sock"
  docker context use lima-{{.Name}}
  docker run hello-world
  ------
env:
   PYTHON3_PREFIX: /usr
   PYTHON3_VERSION: python3.12
   ZMQ_LIBRARY_PREFIX: /usr/lib/x86_64-linux-gnu/
   ZMQ_INCLUDE_PREFIX: /usr/include
   PROTOBUF_LIBRARY_PREFIX: /usr/lib/x86_64-linux-gnu
   PROTOBUF_INCLUDE_PREFIX: /usr/include/
   PROTOBUF_BIN: /usr/bin/protoc
   USE_BAZEL_VERSION: 8.4.2
   JAVA_PREFIX: /usr/lib/jvm/java-17-openjdk-amd64
   EXASOL_TELEMETRY_DISABLE: yes
mounts:
  - location: "~"
    writable: true
