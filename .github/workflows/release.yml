name: Create Draft Release

on:
  push:
    tags:
      - '**'
  workflow_dispatch:
    inputs:
      tag:
        description: "Existing git tag to release (for example: 1.2.3)"
        required: true
        type: string

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-24.04
    env:
      TAG: ${{ inputs.tag || github.ref_name }}
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && format('refs/tags/{0}', inputs.tag) || github.ref }}

      - name: Setup Python & Poetry Environment
        uses: exasol/python-toolbox/.github/actions/python-environment@v9
        with:
          python-version: "3.10"
          poetry-version: "2.3.0"

      - name: Validate release tag
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          poetry run -- nox -s validate-release -- --tag "$TAG"

      - name: Create draft release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          CHANGES_FILE="doc/changes/changes_${TAG}.md"
          gh release create "$TAG" \
            --draft \
            --title "$TAG" \
            --notes-file "$CHANGES_FILE"
